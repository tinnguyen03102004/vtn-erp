// ================================================================
// Bridge: Re-export from @vtn/shared package
// Existing imports like `import { cn } from '@/lib/utils'` continue to work.
// New code should import directly from '@vtn/shared'.
// ================================================================
export { cn, formatCurrency, formatDate, formatRelativeTime, getInitials, truncate, escapeHtml } from '@vtn/shared'
